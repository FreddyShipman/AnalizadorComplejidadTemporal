package analizadorcomplejidadtemporal.algoritmos;

import java.math.BigInteger;

/**
 * Contiene los algoritmos en su versión recursiva:
 * - Fibonacci
 * - Potencia
 * - Factorial
 */
public class AlgoritmosRecursivos {

    /**
     * Fibonacci Recursivo: O(2^N)
     * Debe limitarse a N <= 40 para evitar que la aplicación se congele.
     */
    public static BigInteger fibonacci(int n) {
        return BigInteger.ZERO; // Placeholder
    }

    /**
     * Potencia Recursiva: O(N)
     */
    public static double potencia(double base, int exponente) {
        return 0; // Placeholder
    }

    /**
     * Factorial Recursivo: O(N)
     */
    public static BigInteger factorial(int n) {
        return BigInteger.ONE; // Placeholder
    }
}
